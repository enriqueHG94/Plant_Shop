# Replace these values with your real PostgreSQL credentials
db_config = {
    'user': 'your_user',
    'password': 'your_password',
    'host': 'localhost', # or the IP address of your PostgreSQL server if remote
    'port': '5432', # PostgreSQL's default port
    'database': 'your_database_name'
}

db_config_silver = {
    'user': 'your_user',
    'password': 'your_password',
    'host': 'localhost', # or the IP address of your PostgreSQL server if remote
    'port': '5432', # PostgreSQL's default port
    'database': 'your_database_name'
}

db_config_gold = {
    'user': 'your_user',
    'password': 'your_password',
    'host': 'localhost', # or the IP address of your PostgreSQL server if remote
    'port': '5432', # PostgreSQL's default port
    'database': 'your_database_name'
}
